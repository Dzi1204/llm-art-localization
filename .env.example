# Azure AI Document Intelligence
# Set endpoint to switch from EasyOCR to Azure automatically.
# Auth: leave KEY blank to use Managed Identity / DefaultAzureCredential (recommended).
#       Run 'az login' locally — no key needed.
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=
AZURE_DOCUMENT_INTELLIGENCE_KEY=

# ── Translation backend (pick one) ─────────────────────────────────────────────

# Option 1: Azure AI Foundry (preferred — enterprise auth, no personal key)
# Auth: DefaultAzureCredential (Managed Identity / az login)
# Comment out or leave blank to fall back to OpenAI.
AZURE_FOUNDRY_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com/models
AZURE_FOUNDRY_MODEL=claude-sonnet-4-6

# Option 2: Azure OpenAI (dev fallback — used only if AZURE_FOUNDRY_ENDPOINT is not set)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_MODEL=
AZURE_OPENAI_API_VERSION=

# Option 3: OpenAI (dev fallback — used only if AZURE_FOUNDRY_ENDPOINT is not set)
OPENAI_API_KEY=
OPENAI_MODEL=

# ───────────────────────────────────────────────────────────────────────────────

# QE Service (dev) — quality scoring after translation
# Auth: paste manually obtained Bearer token below
# Correct URL format: https://llm-quality-estimation-dev.azurewebsites.net/
#                     (no .scm. in the hostname)
QE_ENDPOINT=
QE_BEARER_TOKEN=
QE_SCORE_THRESHOLD=0.7

# Pilot language
TARGET_LANGUAGE=it-IT
